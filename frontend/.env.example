# Copy to `.env` for local dev. On Railway, set this variable on the **frontend**
# service before build so Vite can embed your deployed API origin.
#
# Example (no trailing slash):
# VITE_API_BASE_URL=https://your-backend-service.up.railway.app

VITE_API_BASE_URL=
